module.exports = {
	name: 'restart',
    description: 'restart bot',
    aliases:["res"],
	execute(message, args) {
      
        if (message.author.id=="") {
            // message.channel.send(url)
        }
      
	},
};